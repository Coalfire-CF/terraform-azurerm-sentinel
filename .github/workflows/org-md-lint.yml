name: Org Release
on:
    pull_request:
      paths:
        - '**.md'
      branches:
        - main
    workflow_call:

jobs:
  create-release:
    uses: Coalfire-CF/Actions/.github/workflows/org-markdown-lint.yml@main