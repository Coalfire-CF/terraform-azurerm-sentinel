name: Org Terraform Validate

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    types:
      - opened
    paths:
      - "**.tf"
      - "**.tfvars"
      - "**.hcl"
      - ".terraform.lock.hcl"
      - "**/terraform/**"

jobs:
  create-release:
    uses: Coalfire-CF/Actions/.github/workflows/org-terraform-validate.yml@f1183271357ddaaa090a33c1b722133f874ef86d # v0.8.2
    with:
      terraform_version: "1.13.3"
    secrets: inherit
